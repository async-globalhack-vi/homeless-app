%header.flex
  %h1.logo Ruby Slippers

  %nav.flex
    %p
      = current_user.email
    %span &#124;
    %p
      = link_to 'Log Out', logout_path

%section
  %h2.underline Care Coordinator Dashboard

  = geo_chart AssistanceProvider.group(:zip).count, library: {region: 'US', resolution: 'provinces', displayMode: "markers"}

  -#%event-map

  %care-coordinator

